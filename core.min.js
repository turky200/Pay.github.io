window.addEventListener("load", function () {

    const header = document.querySelector(".header-middle");
    if (!header) return;

    function handleScroll() {
        if (window.scrollY > 50) {
            header.classList.add("scrolled");
        } else {
            header.classList.remove("scrolled");
        }
    }

    window.addEventListener("scroll", handleScroll);
    handleScroll();

});






// ============================
// Ultra Subtle Particles
// ============================

const canvas = document.createElement("canvas");
canvas.id = "bgParticles";
document.body.appendChild(canvas);

const ctx = canvas.getContext("2d");

canvas.style.position = "fixed";
canvas.style.top = 0;
canvas.style.left = 0;
canvas.style.width = "100%";
canvas.style.height = "100%";
canvas.style.zIndex = "-1";
canvas.style.pointerEvents = "none";

function resize() {
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
}
resize();
window.addEventListener("resize", resize);

let particles = [];
for (let i = 0; i < 80; i++) {
    particles.push({
        x: Math.random() * canvas.width,
        y: Math.random() * canvas.height,
        r: Math.random() * 1.5,
        dx: (Math.random() - 0.5) * 0.2,
        dy: (Math.random() - 0.5) * 0.2
    });
}

function animate() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    ctx.fillStyle = "rgba(146,0,219,0.5)";

    particles.forEach(p => {
        p.x += p.dx;
        p.y += p.dy;

        if (p.x < 0 || p.x > canvas.width) p.dx *= -1;
        if (p.y < 0 || p.y > canvas.height) p.dy *= -1;

        ctx.beginPath();
        ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
        ctx.fill();
    });

    requestAnimationFrame(animate);
}

animate();



// ==========================
// Background Mode Switcher
// ==========================

function setBgMode(mode) {
    document.body.classList.remove(
        "bg-aurora",
        "bg-neon",
        "bg-mouse",
        "bg-webgl"
    );

    document.body.classList.add(mode);
}





// ==========================
// Inject Trust Box
// ==========================

const productDetails = document.querySelector(".product-details");

if (productDetails) {
    const trust = document.createElement("div");
    trust.classList.add("trust-box");
    trust.innerHTML = "⚡ تسليم فوري – 🔒 دفع آمن 100% – 💬 دعم سريع";
    productDetails.appendChild(trust);
}



const priceBox = document.querySelector(".product-price");

if (priceBox) {
    const note = document.createElement("div");
    note.style.marginTop = "6px";
    note.style.fontSize = "13px";
    note.style.color = "#9a9ab3";
    note.innerHTML = "⚡ تسليم فوري – 🔒 دفع آمن 100%";
    priceBox.appendChild(note);
}
